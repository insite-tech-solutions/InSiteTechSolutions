/**
 * Application constants
 */

// Header height in pixels - should match the CSS custom property --header-height
export const HEADER_HEIGHT = 80;

// Header height as a CSS value (with 'px' unit)
export const HEADER_HEIGHT_CSS = `${HEADER_HEIGHT}px`;

// Common header-related values
export const HEADER_OFFSET = HEADER_HEIGHT;
export const HEADER_OFFSET_CSS = HEADER_HEIGHT_CSS; 